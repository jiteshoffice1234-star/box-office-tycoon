import type { GameState } from '../game/types'

export function Awards(_s: { state: GameState }) {
  return (
    <div className="empty">
      <p>Awards system has been removed.</p>
    </div>
  )
}
